export function autocomplete(data, args) {
    return [...data.servers];
}

/** @param {import("../../.").NS } ns */
export async function main(ns) {
    //IF money > mostExpensiveServer, buy a new server
    //IF home RAM is affordable, buy it
    //IF home CORE is affordable, buy it
    //IF TOR is affordable, buy it
    //IF TOR PROGRAMS ARE AFFORDABLE, BUY THEM
    //IF Stock Market API is affordable, buy it
}